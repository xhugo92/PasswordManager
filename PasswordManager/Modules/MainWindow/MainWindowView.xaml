<Window x:Class="PasswordManager.Modules.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.Modules"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" >
    <Grid Background="Red" >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid x:Name="Content" Background="Aqua">
            
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Home" Command="{Binding NavigateHomeCommand}"/>
            <Button Content="Add" Command="{Binding NavigateAddCommand}"/>
            <Button Content="Gallery" Command="{Binding NavigateGalleryCommand}"/>
            <Button Content="Config" Command="{Binding NavigateConfigurationCommand}"/>
        </StackPanel>
        
    </Grid>
</Window>
